# Demo Docs

This sandbox validates the repo-side agent workflow.

The corrected implementation now makes `add(a, b)` return the arithmetic sum of its two inputs. The validation check covers both positive and mixed-sign inputs.
